== Basics

Styx uses the Nix expression language.
The Nix expression language is a lazily evaluated functional language with uncommon semantics. +
It is recommended to read the link:http://nixos.org/nix/manual/#ch-expression-language[Nix expression language chapter] of the Nix manual to get more familiar with it.

=== Hello world!

Let's look at a very basic example of a styx site `site.nix` file. A real site
also needs a `flake.nix` that passes `pkgs`, `styxlib`, and `styxthemes` into
this function; the site template provides that wiring.

[source, nix]
.Hello world!
----
{ pkgs, styxlib, styxthemes ? {}, extraConf ? {} }:
styxlib.generation.mkSitePackage { # <1>
  inherit styxlib;
  themes = [ styxthemes.generic-templates ]; # <2>
  config = [ ./conf.nix extraConf ];
  extraEnv = { inherit pkgs; };
  body = loaded: { # <3>
    data = {};
    pages.index = { # <4>
      title    = "Styx example page";
      content  = "<p>Hello world!</p>"; # <5>
      path     = "/index.html"; # <6>
      template = loaded.templates.page.full; # <7>
      layout   = loaded.templates.layout; # <8>
    };
  };
}
----

<1> `site.nix` is a function returning a site derivation via `mkSitePackage`.
    Its arguments are supplied by the site's `flake.nix`: `pkgs`, `styxlib`, and
    `styxthemes`.
<2> Themes are paths or packages from the styx theme set.
<3> `body` receives the loaded theme context and returns `data` and `pages`.
<4> Pages are declared in the `pages` attribute set. Here `index` is the only
    page generated.
<5> Page content. Data loaders usually populate this attribute; here it is set
    inline.
<6> The URL path of the generated page.
<7> The page template from the loaded theme.
<8> The page layout from the loaded theme.

Build and preview the site from the site directory:

[source, sh]
.Generating a preview of the "Hello world!" site
----
$ nix build
$ nix run .#serve
----


TIP: Even if this is a simple example, nix language semantics can be confusing. +
In a nutshell, to build a site, `mkSite` evaluates `page.layout (page.template page)` and output the result in `page.path` for every page in `pageList`. +
That is the reason `layout`, `template` and `path` are required attributes of a page set.

NOTE: Nix expression language is lazily evaluated, so infinite data structures are not a problem (unless some code tries to evaluate them).

=== Files

In its simplest form, styx just needs a `site.nix` and a `flake.nix` to generate a site like presented in the <<Hello world!>> example.

The `nix flake init -t github:styx-static/styx#default` template generates a few more files for convenience.

[source]
----
├── flake.nix   # <1>
├── conf.nix    # <2>
├── readme.md   # <3>
├── site.nix    # <4>
├── data/       # <5>
└── themes/     # <6>
----

<1> `flake.nix` wires `site.nix` into `packages`, `checks`, and `apps` (`serve`,
    `linkcheck`, `deploy-gh-pages`).
<2> `conf.nix` is the main configuration file, see <<Configuration>> for details.
<3> `readme.md` provides some basic information about how to start using styx.
<4> `site.nix` is the main file for site generation, see <<site.nix,site.nix>> for details.
<5> `data/` is an empty directory meant to hold site data.
<6> `themes/` is an empty directory meant to hold custom themes.

NOTE: The file structure of a styx site is totally free and can be changed according to needs. +
What matters is that your `flake.nix` imports `site.nix` with the correct arguments.


[[site.nix]]
=== site.nix

The `site.nix` used in the <<Hello world!>> example contains only enough code to generate a single page. A standard `site.nix` is usually more structured.

A complex example of a `site.nix` if the showcase theme example `site.nix` that uses almost every feature available.

A standard `site.nix` is divided in multiple sections, each section handling a particular topic. +
Standard sections are:

- <<site.nix-init,Init>>
- <<site.nix-themes,Themes>>
- <<site.nix-data,Data>>
- <<site.nix-pages,Pages>>
- <<site.nix-site,Site>>

[[site.nix-init]]
==== Init

This section is the basic setup of styx, it should not be changed and used as is for most setups.

[source, nix]
.Standard Init section
----
/*-----------------------------------------------------------------------------
   Init

   Initialization of Styx, should not be edited
-----------------------------------------------------------------------------*/

{
  pkgs,
  styxlib,
  styxthemes ? {},
  extraConf ? {},
}: rec {
----


[[site.nix-themes]]
==== Themes and site output

Most sites use link:library.html#lib.generation.mkSitePackage[`mkSitePackage`], which loads themes, merges configuration, builds `pageList`, and calls `mkSite`.

[source, nix]
.Standard site.nix with mkSitePackage
----
styxlib.generation.mkSitePackage {
  inherit styxlib;
  config = [ ./conf.nix extraConf ];
  themes = [ styxthemes.generic-templates ./themes/my-theme ];
  extraEnv = { inherit pkgs; };
  body = loaded: rec {
    data = { ... };
    pages = { ... };
  };
}
----

`config` is a list of NixOS-style modules or plain `{ config = ...; }` attrsets. Items at the end have higher priority. +
`extraConf` is typically passed from the site's `flake.nix` (for example to override `siteUrl` when serving locally). +
`body` receives the loaded theme context (`loaded.templates`, `loaded.conf`, …) and returns `{ data, pages }`.

Advanced sites can call `styxlib.themes.load` directly and wire `pagesToList` / `mkSite` themselves.

[[site.nix-data]]
==== Data

The data section is responsible for loading the data used in the site.

The <<Data>> section explains in detail how to manage data.

[source, nix]
.Standard data section
----
/*-----------------------------------------------------------------------------
   Data

   This section declares the data used by the site
-----------------------------------------------------------------------------*/

  data = {
    about = lib.data.loadFile { file = ./pages/about.md; inherit env; }; # <1>
  };
----

<1> Example of loading a Markdown file with the `loadFile` function.

[[site.nix-pages]]
==== Pages

The pages section is used to declare the pages that will be generated by `mkSite`. +
Even if `mkSite` expects a page list, it is usually declared as an attribute set for convenience.

There are multiple functions available to generate different type of pages, but a page is ultimately an attribute set with at least the `layout`, `template` and `path` attribute defined.

The <<Pages>> section explains in detail how to create pages.

[source, nix]
.Standard pages section
----
/*-----------------------------------------------------------------------------
   Pages

   This section declares the pages that will be generated
-----------------------------------------------------------------------------*/

  pages = {

    about = {
      path     = "/about.html";
      template = templates.page.full;
      layout   = templates.layout;
    } // data.about; # <1>

  };
----

<1> `//` is the operator to merge attribute sets, this merge the `data.about` data attribute set in the `pages.about` page attribute set.

IMPORTANT: `path` must start with a `/`.

[[site.nix-site]]
==== Site

`mkSitePackage` returns `{ site, conf, files, templates, pages, data, pageList, ... }`. Flake packages typically expose `.site` as the default output.

[source, nix]
.Manual mkSite wiring (advanced)
----
pageList = lib.generation.pagesToList { inherit pages; };
site = lib.generation.mkSite { inherit files pageList; };
----

`pagesToList` converts the pages attribute set to a flat list (expanding page groups from `mkPageList`).

NOTE: `files` comes from the loaded theme. +
`inherit` is shorthand: `{ inherit a; }` is equivalent to `{ a = a; }`.

==== site.nix in a nutshell

====
`site.nix` is a function taking `pkgs`, `styxlib`, and optionally `styxthemes` and `extraConf`. It returns an attribute set with a `site` derivation (via `mkSitePackage` or manual `mkSite`).
====

====
`mkSite` evaluates each page with `page.layout (page.template page)` and writes the result to `page.path`, then copies theme static files into the output store path.
====

